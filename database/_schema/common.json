{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://awesomeledlist.com/schema/common.json",
  "definitions": {
    "status": {
      "type": "string",
      "enum": ["active", "discontinued", "end-of-life", "limited", "unknown"]
    },
    "url": {
      "type": "string",
      "format": "uri"
    },
    "image_object": {
      "type": "object",
      "additionalProperties": false,
      "required": ["file"],
      "properties": {
        "file": {
          "type": "string",
          "description": "Filename in public/database-images/<category>/"
        },
        "source": {
          "type": "string",
          "format": "uri",
          "description": "Page the image came from — original author/source attribution"
        },
        "credit": { "type": "string", "description": "Author/owner to credit" },
        "license": { "type": "string" }
      }
    },
    "image": {
      "description": "Primary image: a bare filename, or an object carrying source/credit attribution (preferred for new images)",
      "oneOf": [{ "type": "string" }, { "type": "null" }, { "$ref": "#/definitions/image_object" }]
    },
    "images": {
      "description": "Array of images: bare filenames, or objects carrying source/credit attribution (preferred for new images)",
      "type": ["array", "null"],
      "items": {
        "oneOf": [{ "type": "string" }, { "$ref": "#/definitions/image_object" }]
      }
    },
    "price": {
      "description": "Bare numbers/strings are USD. Non-USD prices use {amount, currency}; display keeps the original currency, while sorting compares semi-recent USD equivalents (app/lib/currency.ts).",
      "oneOf": [
        { "type": "number", "minimum": 0 },
        { "type": "string", "pattern": "^\\$?[0-9,]+\\.?[0-9]*$" },
        { "type": "string", "enum": ["free", "varies", "contact", "unknown"] },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["amount"],
          "properties": {
            "amount": { "type": "number", "minimum": 0 },
            "currency": {
              "type": "string",
              "pattern": "^[A-Z]{3}$",
              "description": "ISO 4217 code (EUR, GBP, …); omit for USD"
            }
          }
        }
      ]
    },
    "voltage": {
      "oneOf": [{ "type": "number" }, { "type": "string", "pattern": "^[0-9]+\\.?[0-9]*\\s*V?$" }]
    },
    "current": {
      "oneOf": [
        { "type": "number" },
        { "type": "string", "pattern": "^[0-9]+\\.?[0-9]*\\s*(m?A)?$" }
      ]
    },
    "year": {
      "oneOf": [{ "type": "integer", "minimum": 1990, "maximum": 2030 }, { "type": "null" }]
    },
    "datasheet_mirror": {
      "type": ["object", "null"],
      "description": "Repo-managed copy of the datasheet PDF on Cloudflare R2. Written by CI (scripts/mirror-datasheets.ts) - do not edit by hand. The original author/source URL stays in datasheet.url.",
      "additionalProperties": false,
      "required": ["url", "sha256", "retrieved", "source"],
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Public R2 URL of the mirrored PDF"
        },
        "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "retrieved": { "type": "string", "format": "date" },
        "source": {
          "type": "string",
          "format": "uri",
          "description": "datasheet.url at fetch time; a changed source triggers re-mirroring"
        }
      }
    },
    "dead_links": {
      "type": ["array", "null"],
      "description": "URLs elsewhere in this entry that no longer work. Detail pages keep the original for attribution but link to `archive` instead — or to the newest Wayback Machine capture when `archive` is omitted. Set `archive` explicitly when the domain changed hands and the latest capture shows the wrong site.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["url"],
        "properties": {
          "url": {
            "type": "string",
            "format": "uri",
            "description": "The dead URL, exactly as it appears elsewhere in this entry"
          },
          "archive": {
            "type": "string",
            "format": "uri",
            "description": "Curated archived copy (e.g. a Wayback capture from before the domain changed hands)"
          },
          "checked": {
            "type": "string",
            "format": "date",
            "description": "When the link was last confirmed dead"
          },
          "notes": { "type": "string" }
        }
      }
    },
    "related": {
      "type": ["array", "null"],
      "description": "Related products (replacement, predecessor, variant, accessory, etc.). For a discontinued product, a `replacement` item points at its successor. Declare each relationship on one side only; detail pages derive the reverse direction automatically.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["type"],
        "anyOf": [{ "required": ["ref"] }, { "required": ["name"] }],
        "properties": {
          "type": {
            "type": "string",
            "enum": ["replacement", "predecessor", "variant", "accessory", "related"]
          },
          "ref": {
            "type": "string",
            "pattern": "^[a-z0-9-]+/[a-z0-9][a-z0-9._-]*$",
            "description": "Internal reference `<category>/<entry-slug>` (the YAML filename without extension). Preferred whenever the product is in this database; scripts/validate.ts checks that it resolves."
          },
          "name": {
            "type": "string",
            "description": "Display name. Required when the product is not in the database (no `ref`); otherwise an optional label override."
          },
          "url": {
            "type": "string",
            "format": "uri",
            "description": "External link for products not in the database"
          },
          "notes": { "type": "string" }
        }
      }
    }
  }
}
