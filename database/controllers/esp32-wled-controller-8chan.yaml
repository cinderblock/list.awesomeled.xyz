name: ESP32 WLED Controller 8chan
status: active
creator:
  name: Giovanni's Domotica Store
  page: https://www.tindie.com/products/giovannicas/esp32-wled-controller-8chan/
core:
  family: ESP32
inputs:
  physical:
    Ethernet: true
  standalone: true # runs WLED firmware standalone
outputs:
  count: 8
  pixels:
    max_per_output: 600 # 4800 total across 8 outputs
  clocked: selectable # single and clock (both pixel types)
power:
  voltage:
    max: 12
features:
  - GPIO / Relay outputs
price: 45
wled_compatible: true
