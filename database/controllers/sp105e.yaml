name: SP105E
status: active
creator: Sperll
vendors:
  - Amazon
inputs:
  physical:
    Bluetooth: true # configured via smartphone app
  standalone: true # plays patterns from onboard memory, controlled over Bluetooth
outputs:
  count: 1
  connector: Screw
  pixels:
    max_per_output: 2048
  clocked: selectable # supports single-wire and clocked pixel types
power:
  voltage:
    max: 24
price: 18
