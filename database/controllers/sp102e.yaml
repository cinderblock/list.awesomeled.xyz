name: SP102E
status: active
creator: Sperll
vendors:
  - Amazon
inputs:
  physical:
    buttons: true # onboard buttons + display for standalone pattern control
  standalone: true # built-in patterns selectable via onboard buttons/display
outputs:
  count: 1
  connector: Screw
  clocked: selectable # supports single-wire (WS2812) and clocked (APA102/SPI) ICs
  pixels:
    max_per_output: 1024
power:
  voltage:
    max: 12
price: 13
